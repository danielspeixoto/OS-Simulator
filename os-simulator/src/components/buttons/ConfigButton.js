import React from 'react'

const ConfigButton = (props) => {

	return (
		<div>
			<button
				className='config-button'
				style={props.style}>

			</button>
		</div>
	)
}

export default ConfigButton